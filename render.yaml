services:
  - type: web
    name: savant-video-downloader
    runtime: docker
    plan: free
    healthCheckPath: /
    dockerCommand: gunicorn app:app --bind 0.0.0.0:10000 --workers 1 --threads 4 --timeout 600
